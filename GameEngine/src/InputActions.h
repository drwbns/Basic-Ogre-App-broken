// Describes Input actions such as keyPressed, keyReleased, MouseUp, MouseDown
#ifndef InputActions_h
#define InputActions_h

enum InputAction {
	MOUSEUP,
	MOUSEDOWN,
	KEYUP,
	KEYDOWN,
	NULLACTION
};

#endif